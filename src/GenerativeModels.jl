module GenerativeModels

using POMDPs

export generate_s,
       generate_o,
       generate_sr,
       generate_so,
       generate_or,
       generate_sor,
       initial_state

warn("GenerativeModels.jl has been merged into POMDPs.jl. Please import the functions from POMDPs rather than from GenerativeModels.")

end # module
